# 商品魅力向上の実装記録

## 1・2: 紹介・初回体験（2026-09-05）

日英の紹介・CLI・マニュアルを明示的なGit操作と整合させ、初回の一括登録と閉じられる案内を追加。Finderの保存失敗時は登録を戻し、選択を保持する。最終コミット列へ改名。

実TUIの4場面を28秒のSVGに生成。PTYのUTF-8分割読み取りを修正し、SVGの代替フォントでもセル幅を維持。日英Home画像とマニュアルを生成元から更新。

検証: fmt、Clippy（全target、警告なし）、ユニット211件・統合17件成功。日英の実TUIで登録・要対応・差分・シェルと終了後の復帰を確認。Chromiumで日英4場面を目視し、文字化け・罫線のはみ出しを修正。案内の通常・狭幅・低高の描画をテスト。1バイト単位に分割したUTF-8入力も復元できることを確認。

この環境ではmacOSの既存targetを利用せず、Linux用に `/tmp/gdt-target` で検証した。初回ビルドの失敗と、狭幅時に省略された名前を全文一致で期待したテスト失敗の完全ログは `/tmp/gdt-stage12-*.log` に保持。初見の協力者による使いやすさの評価は未実施。

## 3・4: Homeの判断材料（2026-09-05）

`feature/home-attention-context` のworktreeで実装。現在のグループ・検索範囲を対象に、要対応（リポジトリ単位）・未コミット・同期差分・ローカル未取得を別々に集計。選択行に理由と操作、ローカル取得時刻を表示し、狭い画面では折りたたむ。マウスは描画した表の境界を参照する。

GitHub情報はプロセス内で5分キャッシュし、失敗後は1分待機（次の再読み込みで再試行）。同じパスへの同時呼び出しをまとめ、CI/PRの取得失敗・未認証・未導入・実行なし・古いキャッシュを区別する。CIの全リポジトリ対象・ブランチ・取得時刻、PRの100+を表示。CでGitHubのHTTPS実行URLを開く。SSHのGitHub取得は非対応と表示する。

検証: fmt、Clippy警告なし、ユニット216件・統合17件成功。日英実TUIとサンプルghで別ブランチCI、100+、ローカル再読み込みでghが再実行されないことを確認し、Chromiumで選択行の詳細を目視。キャッシュ期限・失敗時の待機・状態分類・古い値の保持・集計範囲・動的なマウス位置をテスト。

## 配信状況

1・2のcommitは `25d05c5`。pushはHTTPS認証不足で失敗し、`gh auth status` でも環境のGH_TOKENが無効と判定された。認証情報は表示・変更していない。実装をローカルで続行する。

## 5: 作業ツール（2026-09-05）

`feature/work-tools` のworktreeでOメニューを追加。シェル、設定済みエディタ、lazygit、GitUIを選び、cでエディタ設定、wで終了待機を切り替える。GUIは既定で待機しない。端末内ツール・シェル終了後は画面と入力を復帰して対象リポジトリを再取得する。引用符付き引数・空白パスに対応し、相対実行ファイルと相対PATHエントリを拒否。未導入とSSH非対応を表示する。

検証: fmt、Clippy警告なし、ユニット218件・統合17件成功。実TUIとサンプル実行ファイルで、空白パスのcwd、端末内ツールからの復帰と入力、GUIへのパス引数、未導入ツールのエラーを確認。初回Clippyのlet-and-return指摘は修正し、完全ログを保持。

## 6

未実装。横断Worktree一覧を続ける。
